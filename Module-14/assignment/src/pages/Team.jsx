import Breadcrumb from "../components/Breadcrumb";


const Team = (props) => {
    return (
        <div>
            <Breadcrumb breadcrumb={props.breadcrumb} />
        </div>
    );
};

export default Team;